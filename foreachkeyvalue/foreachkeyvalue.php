$userName, 'Street'=>$streetAddress, 'City'=>$cityAddress); // foreach ($variable as $key => $value) { // # code... // } foreach ($customers as $customer => $value) { echo '
'. $customer .': '. $value; } ?>
